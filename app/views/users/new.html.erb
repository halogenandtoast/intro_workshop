<%= form_for(@user) do |form| %>
  <%= render "form_errors", :errors => @user.errors %>
  <div>
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>
  <%= form.submit "Sign up!" %>
<% end %>
